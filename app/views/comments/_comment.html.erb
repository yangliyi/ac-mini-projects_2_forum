<div class="row">
  <div class="col-md-6">
    <table class="table">
      <% @post.comments.each do |comment|%>
      <tr>
        <td><%= comment.user.username %>：</td>
        <td><%= comment.comment %></td>
        <td>
          <%= link_to '刪除留言', post_comment_path(@post, comment), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>



